import React from 'react';
import {View, Text} from 'react-native';

const Channels = () => {
  return (
    <View>
      <Text>Channels</Text>
    </View>
  );
};

export default React.memo(Channels);
